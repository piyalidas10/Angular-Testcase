import { AgeRangeFilterPipe } from './age-range-filter.pipe';

describe('AgeRangeFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new AgeRangeFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
